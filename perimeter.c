#include <stdio.h>

int main() {
    int length = 50;
    int width = 20;
    int perimeter;

    perimeter = 2 * (length + width);

    printf("The perimeter of the rectangle is: %d\n", perimeter);

    return 0;
}
